<USER_REQUEST>
Upgrade Geomatrix to use REAL DATA and REAL ML predictions.

- Remove all synthetic/mock/hardcoded project, KPI, chart, risk, and prediction data.
- Add a simple real-data ingestion system for permitted public/official sources such as Bhoomi Rashi and data.gov.in.
- Store source URL, source record ID, and update time for every imported record.
- Keep SQLite for local development and prepare PostgreSQL/PostGIS support.
- Make the GIS map use only real project coordinates.
- Build an ML pipeline using real historical labeled delay data; never create fake labels or predictions.
- Use Random Forest/XGBoost with proper evaluation and add SHAP explanations.
- Dashboard and project pages must use real database/API values.
- Show “Data unavailable” when required real data is missing.
- Keep Google authentication, existing UI, Light/Dark/System themes, and Leaflet.
- Do not add unnecessary frameworks or rewrite working features.

Then test everything:
1. Run all automated tests.
2. Start the app.
3. Test login, dashboard, projects, GIS, data import, prediction, SHAP, themes, and logout in a real browser.
4. Check terminal and browser console errors.
5. Fix every error and rerun the tests.

Do not claim “real prediction” unless it is produced by a trained model using real labeled historical data.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-18T22:57:25+05:30.
</ADDITIONAL_METADATA>
<USER_SETTINGS_CHANGE>
The user changed setting `Model Selection` from None to Claude Sonnet 4.6 (Thinking). No need to comment on this change if the user doesn't ask about it. If reporting what model you are, please use a human readable name instead of the exact string.
</USER_SETTINGS_CHANGE>
===PROMPT===
<USER_REQUEST>
You are upgrading Geomatrix V2 — a Python FastAPI + SQLAlchemy + SQLite + 
Leaflet + Jinja2/HTML project. Do NOT migrate to any other framework. 
Do NOT add React, Next.js, Prisma, or a separate ML sidecar process.

────────────────────────────────────────
STEP 0 — AUDIT FIRST
────────────────────────────────────────
Before writing any code, read every file in the repository and produce a 
full audit report listing:
- Every hardcoded/synthetic value (fake projects, KPIs, risk scores, 
  SHAP values, confusion matrices, chart data, Math.random() equivalents)
- Every route that returns fake data
- Every file that must be changed and why

Do not start implementation until the audit is complete.

────────────────────────────────────────
STEP 1 — REMOVE ALL SYNTHETIC DATA
────────────────────────────────────────
Remove every hardcoded fake value identified in Step 0.
Keep TypeScript/Python type definitions and legitimate test fixtures.
Replace fake API responses with real DB queries.
If the DB is empty, return { "data": [], "message": "No data imported yet" }.
Never return made-up numbers.

────────────────────────────────────────
STEP 2 — DATABASE (SQLite, production-ready schema)
────────────────────────────────────────
Keep SQLite for local dev. Prepare for PostgreSQL/PostGIS in production 
(document the switch but do not implement it now).

Add to every imported record:
- source_url (TEXT)
- source_record_id (TEXT)
- imported_at (DATETIME)
- updated_at (DATETIME)
- validation_status (TEXT: "valid" | "invalid" | "pending")

Add a ModelRun table:
- id, trained_at, algorithm, accuracy, f1_score, 
  feature_names (JSON), sample_count, model_path

Add a DataIngestionLog table:
- id, source_name, triggered_at, records_fetched, 
  records_saved, records_skipped, error_message

────────────────────────────────────────
STEP 3 — REAL DATA INGESTION
────────────────────────────────────────
Source 1 — data.gov.in:
  Use their open unauthenticated REST API.
  Target dataset: infrastructure/land acquisition datasets.
  Endpoint: POST /api/ingest/datagovIn
  On failure: log to DataIngestionLog, return error — do not crash.

Source 2 — Bhoomi Rashi (Karnataka land records):
  Bhoomi Rashi has no public API.
  Implement CSV upload only.
  Endpoint: POST /api/ingest/upload (multipart CSV)
  Parse, validate coordinates, upsert into DB.
  Return: { records_parsed, records_saved, records_skipped, errors[] }
  Never use Math.random() or fake row counts.

────────────────────────────────────────
STEP 4 — GIS MAP
────────────────────────────────────────
Keep Leaflet. 
Map markers must come only from real latitude/longitude in the database.
If a project has null/invalid coordinates, skip it — do not show a 
default/hardcoded marker.
Show "No project coordinates available" empty state when DB has no 
valid coordinates.

────────────────────────────────────────
STEP 5 — ML PIPELINE
────────────────────────────────────────
Create ml/ directory:
  ml/
    features.py   — feature extraction from real DB records
    train.py      — trains model, saves model.pkl + model_meta.json
    predict.py    — loads model, returns prediction + confidence
    evaluate.py   — cross-validation, confusion matrix, classification report
    explain.py    — SHAP values from the actual trained model

Rules:
- Use real historical labeled records from the DB (HistoricalDelayRecord table)
- Minimum 10 labeled records required before training is allowed
- If < 10 records: refuse training with clear message, return 
  "Model not trained — insufficient real labeled data" from all predict endpoints
- Start with Logistic Regression baseline, then Random Forest
- Add XGBoost only if Random Forest F1 < 0.70
- Never create synthetic labels
- Never hardcode accuracy metrics — calculate from real evaluation
- Save model_meta.json with: algorithm, trained_at, accuracy, f1, 
  feature_names, sample_count

Endpoint: POST /api/projects/{project_id}/predict-risk
  - Read real project data from DB
  - Extract features using features.py
  - Load trained model
  - Return: risk_score, delay_probability, predicted_delay_days, 
    confidence, model_version, prediction_generated_at
  - If model not trained: return HTTP 503 with honest message
  - Save prediction to DB with timestamp

SHAP endpoint: GET /api/projects/{project_id}/explain
  - Return per-feature SHAP values from actual trained model
  - If model not trained: HTTP 503

────────────────────────────────────────
STEP 6 — DASHBOARD & ALL PAGES
────────────────────────────────────────
Every KPI card, chart, and number must come from real DB queries.
If data is missing, show "Data unavailable" — never a fake number.

Every prediction display must show:
- Data Source + Source URL
- Last Updated timestamp
- Model Version
- Prediction Generated At

────────────────────────────────────────
STEP 7 — KEEP WORKING (DO NOT BREAK)
────────────────────────────────────────
- Google OAuth authentication
- Light / Dark / System theme switching
- All existing UI layout and navigation
- Leaflet map (do not replace with Google Maps or any other library)
- Mobile-responsive layout

────────────────────────────────────────
STEP 8 — TESTING (DO NOT SKIP ANY STEP)
────────────────────────────────────────
Run in this exact order. Fix every failure before moving to the next.

1. pytest — all tests must pass
2. python -m uvicorn main:app --reload — must start with zero errors
3. Browser — login with Google OAuth
4. Browser — dashboard shows "Data unavailable" (no data yet), no JS errors
5. Browser — projects page shows empty state
6. Browser — GIS map shows empty state, no JS errors
7. Browser — upload a real CSV → verify real row counts in response
8. Browser — trigger data.gov.in import → verify real response
9. Browser — ML model page shows "not trained" state
10. Browser — import ≥ 10 historical records → train model → verify 
    real metrics appear (not hardcoded numbers)
11. Browser — run prediction → verify model version + timestamp in response
12. Browser — SHAP page → verify real feature values
13. Browser — switch Light/Dark/System themes → verify no layout breaks
14. Browser — logout → redirects to login page
15. Check browser console — zero errors
16. Check terminal — zero errors

A step counts as PASSING only when the expected output is confirmed.
"No errors" means literally zero errors, not "I didn't notice any".
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-18T23:04:13+05:30.

The user's current state is as follows:
Active Document: c:\Users\rohit\Downloads\geomatrix-sih2026-prototype.worktrees\geomatrix-full-update-production-ready\README_NEW.md (LANGUAGE_MARKDOWN)
Cursor is on line: 172
Other open documents:
- c:\Users\rohit\Downloads\geomatrix-sih2026-prototype.worktrees\geomatrix-full-update-production-ready\app\projects\[id]\page.tsx (LANGUAGE_TSX)
- c:\Users\rohit\Downloads\geomatrix-sih2026-prototype.worktrees\geomatrix-full-update-production-ready\README_NEW.md (LANGUAGE_MARKDOWN)
- c:\Users\rohit\Downloads\geomatrix-sih2026-prototype.worktrees\geomatrix-full-update-production-ready\README.md (LANGUAGE_MARKDOWN)
- c:\Users\rohit\Downloads\geomatrix-sih2026-prototype.worktrees\geomatrix-full-update-production-ready\.env (LANGUAGE_UNSPECIFIED)
</ADDITIONAL_METADATA>
===PROMPT===
<USER_REQUEST>
complete the remanaing tasks 
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-18T23:16:22+05:30.

The user's current state is as follows:
Active Document: c:\Users\rohit\Downloads\geomatrix-sih2026-prototype.worktrees\geomatrix-full-update-production-ready\geomatrix_v2\routers\ml.py (LANGUAGE_PYTHON)
Cursor is on line: 1
Other open documents:
- c:\Users\rohit\Downloads\geomatrix-sih2026-prototype.worktrees\geomatrix-full-update-production-ready\geomatrix_v2\routers\alerts.py (LANGUAGE_PYTHON)
- c:\Users\rohit\Downloads\geomatrix-sih2026-prototype.worktrees\geomatrix-full-update-production-ready\geomatrix_v2\database.py (LANGUAGE_PYTHON)
- c:\Users\rohit\Downloads\geomatrix-sih2026-prototype.worktrees\geomatrix-full-update-production-ready\geomatrix_v2\routers\ingest.py (LANGUAGE_PYTHON)
- c:\Users\rohit\Downloads\geomatrix-sih2026-prototype.worktrees\geomatrix-full-update-production-ready\geomatrix_v2\requirements.txt (LANGUAGE_UNSPECIFIED)
- c:\Users\rohit\Downloads\geomatrix-sih2026-prototype.worktrees\geomatrix-full-update-production-ready\lib\api.ts (LANGUAGE_TYPESCRIPT)
</ADDITIONAL_METADATA>
<USER_SETTINGS_CHANGE>
The user changed setting `Model Selection` from Claude Sonnet 4.6 (Thinking) to Gemini 3.8 Flash (Medium). No need to comment on this change if the user doesn't ask about it. If reporting what model you are, please use a human readable name instead of the exact string.
</USER_SETTINGS_CHANGE>